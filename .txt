## Perequisites :

Must check that following mcp servers are available or not 
- chrome-devtools
- playwright

You've to resolve a particular issue occured in the @WhatsappWatcher watcher , this watcher is used for monitoring the whatsapp using playwright , not whatsapp platform API because whatsapp platform api is very expensive . 

The issue is that the script @WhatsappWatcher/main.py opens browser and navigate me to web.whatsapp.com where i log in with QR code , I clearly see the incoming unread messages in realtime in whatsapp web interface , but the playwright script , however doesnot see the required html elements and attributes , there fore it doesn't see unread messages , thus failing for whatsapp handling aspect 

Use the mcp servers defined above for tasks such as working with browser and inspecting elements tree through chrome devtools . 

Read files :
- @doc.md  - (main hackathon project documentation , must read silver tier requirements)
- @README.md - (project current status and details, currently working on silver tier)
- @WhatsappWatcher  -  watcher for whatsapp handling and monitoring 
- @.qwen/skills/whatsapp-monitoring  -  custom devveloped agent skill for monitoring whatsapp which uses same whatsapp watcher (when the whatsapp watcher is fixed , this should be updated)

## Steps:
- Open the chrome browser using playwright mcp server
- Use the chrome-devtools mcp to identify elements for:
  - Chat list panel
  - Individual chat threads
  - Unread message indicators
  - Message bubbles (incoming vs outgoing)
  - Message text content
- Capture element selectors, classes, and attributes for:
  - Unread message badge (unread count)
  - Chat items with unread status
  - Message containers
  - Timestamp elements

- Compare current selectors in `@WhatsappWatcher` with actual DOM structure
- Document discrepancies:
  - What the script currently looks for
  - What actually exists in the DOM
  - Any dynamic class names or attributes

- Using Chrome DevTools, test each selector the script uses
- Verify if they return the expected elements
- Note any timing issues (elements not loaded immediately)
- Check if shadow DOM is involved

- Monitor DOM mutations when a new message arrives
- Identify:
  - What elements are added/modified
  - What attributes change
  - Any custom data attributes or React component markers
  - Event listeners that could be leveraged

- Based on inspection results, update selectors in `@WhatsappWatcher`
- Focus on:
  - Chat item selection for unread detection
  - Message container selection
  - Message content extraction
- Ensure selectors are:
  - Specific enough to avoid false positives
  - Robust against WhatsApp UI updates
  - Using data-testid attributes if available

- Consider multiple detection methods:
  1. **DOM MutationObserver**: Watch for new message elements
  2. **Polling with improved selectors**: Regular checks with better selectors
  3. **Attribute-based detection**: Look for `aria-label` or `data-*` attributes


After that provide me a complete and consise file `fix-plan.md` which contains 
- list of Issues leading to abnormal behaviour of watcher .
- step by step implementation plan for each issue .


------------------------------------------------------------

send_email
  Description: Send an email via Gmail.

Args:
    to: Recipient email address
    subject: Email subject
    body: Email body content
    from_email: Sender email (optional, uses authenticated account)
    cc: CC recipients (comma-separated)
    bcc: BCC recipients (comma-separated)
    html: Whether body is HTML (default: False)

Returns:
    Dictionary with status and message_id or error
  Parameters:
    - to (required):
    - subject (required):
    - body (required):
    - from_email (optional):
    - cc (optional):
    - bcc (optional):
    - html (optional):

draft_email
  Description: Create a draft email in Gmail (does not send).

Args:
    to: Recipient email address
    subject: Email subject
    body: Email body content
    from_email: Sender email (optional)
    cc: CC recipients (comma-separated)
    html: Whether body is HTML (default: False)

uv run python scripts/mcp-client.py call --url http://localhost:8801/mcp -t draft_email -p "{\"to\": \"affanfarooq824@gmail.com\" ,\"subject\": \"testing digital fte\", \"from_email\": \"affanamir903@gmail.com\" , \"body\": \"hello affan 2 how are you ?\"}"

Returns:
    Dictionary with draft_id and status
  Parameters:
    - to (required):
    - subject (required):
    - body (required):
    - from_email (optional):
    - cc (optional):
    - html (optional):

search_emails
  Description: Search emails in Gmail.

Args:
    query: Gmail search query (e.g., "is:unread", "from:example@gmail.com")
    max_results: Maximum number of results (default: 10)

uv run python scripts/mcp-client.py call --url http://localhost:8801/mcp -t search_emails -p "{\"query\": \"from:affanfarooq824@gmail.com\"}"

Returns:
    Dictionary with list of matching emails
  Parameters:
    - query (required):
    - max_results (optional):

get_email_content
  Description: Get full content of a specific email.

Args:
    message_id: Gmail message ID
    
uv run python scripts/mcp-client.py call --url http://localhost:8801/mcp -t get_email_content -p "{\"message_id\": \"19d1abbab54a4cc4\"}"


Returns:
    Dictionary with email content and attachments info
  Parameters:
    - message_id (required):

mark_as_read
  Description: Mark an email as read.

Args:
    message_id: Gmail message ID

    uv run python scripts/mcp-client.py call --url http://localhost:8801/mcp -t mark_as_read -p "{\"message_id\": \"19d1abbab54a4cc4\"}"

Returns:
    Dictionary with status
  Parameters:
    - message_id (required):

------------------------------------------------------------


First you've to tell me , in the orchesterator , how you're counting if a watcher of mcp server is running ? are you counting process ? answer me in yes or no .

--- 

I think you should have to consider following changes 

- There is not need to maintain history of operations 
- you dont have to track error or anything in it . Neither store error count in it for now .

## Tasks 1:
- Write a document about all the json fields in this json file as a clear and consise readme file `SETTINGS.md` in the project root .

Start implementing the state management 
- Make sure that you create each and every crutial state field in this file 
- All files , watchers , and everything should report to this json file in realtime if anything changes .

## Suggested workflow : 
filesystem started successfully --> update its status in that json file 

mcp server stops ----> update its sttaus in that json file 

first checkout watchers and mcp servers, then **Only proposed me the json structure how state should be looking like and ask permission before writing file** after that if the struture is decided then start implementing json state management in python files .